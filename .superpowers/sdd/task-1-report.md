# Task 1 Report: Add Budget Domain Model

## Completed
- Added `BudgetType` with the required values: `CARRY_OVER`, `OFFERING_INCOME`, `EXPENSE`.
- Added `Budget` in `com.church.operation.entity` with the required fields and standard getters/setters.
- Added `BudgetRepository` with the three required Mongo query methods.

## Verification
- Ran: `cd backend && mvn test -DskipTests`
- Result: `BUILD SUCCESS`

## Notes
- No concerns.
